在RK3588上使用Python实现“最佳性能”，核心难点在于**规避Python的GIL锁（全局解释器锁）以及减少Python层面的内存拷贝**。

为了在8GB内存下跑满多路（例如4路-8路）并保持7x24稳定，不能写在一个单线程 `while` 循环里。

以下是为您设计的**多进程 + 共享内存 + 硬件加速**架构方案。

---

### 1. 整体架构设计图

采用 **生产者-消费者模型**，将重负载任务（解码/存盘）与计算任务（推理）解耦。

```mermaid
graph TD
    subgraph "Process 1: 视频接入与存盘 (I/O密集型)"
    RTSP[RTSP流] -->|GStreamer硬解| Decode[解码器]
    Decode -->|原始帧| SHM[SharedMemory 共享内存]
    Decode -->|硬编码| Save[存盘模块/循环覆盖]
    end

    subgraph "Process 2: AI推理核心 (计算密集型)"
    SHM -->|读取帧| RGA[RGA 预处理 (Resize/Format)]
    RGA -->|RGB/640x640| NPU[RKNN NPU推理]
    NPU -->|结果Tensor| Post[后处理 (CPU)]
    Post -->|结构化数据| ResultQ[结果队列]
    end

    subgraph "Process 3: 业务上报 (网络密集型)"
    ResultQ --> Upload[数据清洗 & MQTT/HTTP上传]
    end
    
    Watchdog[看门狗 & 进程守护] -.-> Process1
    Watchdog -.-> Process2

```

---

### 2. 各部分技术选型 (Python版)

这是在RK3588 Python环境下能达到的性能天花板组合：

| 模块 | 推荐技术/库 | 关键配置 (Magic) |
| --- | --- | --- |
| **视频读取** | **OpenCV (GStreamer后端)** | 必须编译带GStreamer支持的OpenCV。通过 `pipeline` 字符串调用 `mppvideodec` (硬解)。 |
| **预处理** | **rga-python** (或 rk-rga) | 使用Rockchip的RGA库进行 Resize 和 Color Convert。**千万别用 `cv2.resize**`，它会吃掉大量CPU。 |
| **模型推理** | **rknn-toolkit-lite2** | Rockchip官方提供的Python NPU推理库。 |
| **进程通信** | **multiprocessing.SharedMemory** | 零拷贝传输视频帧。传递内存指针而非拷贝整个图片，降低内存和延迟。 |
| **视频存盘** | **GStreamer (Python bindings)** | 独立于OpenCV，直接利用 `mpph265enc` 将流硬编码存为文件，不占用Python主线程资源。 |

---

### 3. Python 代码框架 (伪代码与关键实现)

这个框架包含三个核心类：`CameraSource`、`InferenceEngine`、`SystemManager`。

#### A. 环境准备

确保已安装：

```bash
pip install numpy rknn-toolkit-lite2 opencv-python-headless
# 需要系统级安装 python3-rga 或类似厂商提供的RGA绑定

```

#### B. 核心代码结构

```python
import multiprocessing as mp
from multiprocessing import shared_memory
import numpy as np
import cv2
import time
import os
from rknnlite.api import RKNNLite

# 配置参数
STREAM_URL = "rtsp://admin:password@192.168.1.10:554/stream"
FRAME_SHAPE = (1080, 1920, 3) # 假设1080P
FRAME_SIZE = 1080 * 1920 * 3
SHM_NAME = "camera_0_shm"

# --- 模块1：视频采集与写入共享内存 ---
def camera_process(shm_name, stream_url, signal_q):
    """
    负责解码视频流并将最新帧写入共享内存。
    使用 OpenCV GStreamer Pipeline 进行硬解码。
    """
    # GStreamer Pipeline: RTSP -> rtspsrc -> h264parse -> mppvideodec (硬解) -> appsink
    gst_pipeline = (
        f"rtspsrc location={stream_url} latency=0 ! "
        "rtph264depay ! h264parse ! mppvideodec ! "
        "videoconvert ! video/x-raw,format=BGR ! appsink drop=1"
    )
    
    cap = cv2.VideoCapture(gst_pipeline, cv2.CAP_GSTREAMER)
    
    # 连接预先分配的共享内存
    shm = shared_memory.SharedMemory(name=shm_name)
    np_array = np.ndarray(FRAME_SHAPE, dtype=np.uint8, buffer=shm.buf)
    
    # 独立的存盘线程（伪代码，使用subprocess调用ffmpeg或独立GStreamer pipeline）
    # start_recording_process(stream_url) 
    
    while True:
        ret, frame = cap.read()
        if not ret:
            print("Stream lost, reconnecting...")
            cap.release()
            time.sleep(2)
            cap = cv2.VideoCapture(gst_pipeline, cv2.CAP_GSTREAMER)
            continue
            
        # 直接拷贝到共享内存，Inference进程可直接读取
        # copyto比直接赋值更安全，保持内存地址不变
        np.copyto(np_array, frame)
        
        # 通知推理进程有新帧 (非阻塞)
        if signal_q.empty():
            signal_q.put(time.time())

# --- 模块2：RGA预处理与NPU推理 ---
def inference_process(shm_name, model_path, result_q):
    """
    从共享内存读取，使用RGA缩放，NPU推理
    """
    # 初始化 RKNN
    rknn = RKNNLite()
    rknn.load_rknn(model_path)
    rknn.init_runtime(core_mask=RKNNLite.NPU_CORE_0) # 绑定核心0
    
    # 连接共享内存
    shm = shared_memory.SharedMemory(name=shm_name)
    source_image = np.ndarray(FRAME_SHAPE, dtype=np.uint8, buffer=shm.buf)
    
    # 初始化 RGA (假设使用 rga-python 库)
    # from rga import RgaInterpreter
    # rga = RgaInterpreter()
    
    while True:
        # 1. RGA 预处理: 1920x1080 -> 640x640 (极快, ~2ms)
        # 相比 cv2.resize(source_image, (640,640)) 快10倍以上
        # input_data = rga.resize(source_image, (640, 640))
        
        # (如果没有RGA库，暂时用cv2，这是性能瓶颈点)
        input_data = cv2.resize(source_image, (640, 640)) 
        
        # 2. 推理
        outputs = rknn.inference(inputs=[input_data])
        
        # 3. 后处理 (解析 YOLO 输出)
        # results = post_process(outputs)
        results = {"box": [10, 10, 100, 100], "class": "person"} # 示例
        
        # 4. 发送结果
        result_q.put(results)

# --- 模块3：主控与上传 ---
def main():
    # 1. 创建共享内存块 (8GB内存足够分配几块1080P buffer)
    try:
        shm = shared_memory.SharedMemory(create=True, size=FRAME_SIZE, name=SHM_NAME)
    except FileExistsError:
        shm = shared_memory.SharedMemory(name=SHM_NAME)

    # 2. 队列用于进程间轻量通信
    signal_q = mp.Queue(maxsize=1)
    result_q = mp.Queue()

    # 3. 启动进程
    p_cam = mp.Process(target=camera_process, args=(SHM_NAME, STREAM_URL, signal_q))
    p_inf = mp.Process(target=inference_process, args=(SHM_NAME, "yolov8.rknn", result_q))
    
    p_cam.start()
    p_inf.start()
    
    print("System Started. Listening for results...")
    
    try:
        while True:
            if not result_q.empty():
                data = result_q.get()
                # 执行上传逻辑 (MQTT / HTTP)
                # upload_data(data)
                print(f"Detected: {data}")
            
            # 简单的看门狗逻辑
            if not p_cam.is_alive():
                print("Camera died, restarting...")
                # 重启逻辑...
                
    except KeyboardInterrupt:
        p_cam.terminate()
        p_inf.terminate()
        shm.close()
        shm.unlink() # 必须释放共享内存

if __name__ == "__main__":
    main()

```

---

### 4. 关键性能优化点 (Checklist)

1. **GStreamer Pipeline 是关键**：
* 在 `cv2.VideoCapture` 中使用 `cv2.CAP_GSTREAMER` 是必须的。
* Pipeline 必须包含 `mppvideodec` (硬解)。如果用 `avdec_h264` (软解)，你的CPU会立刻爆满。


2. **存盘策略 (7x24h)**：
* **不要在Python里写视频** (`cv2.VideoWriter` 性能很差)。
* **方案**：使用 `subprocess` 启动一个独立的 FFmpeg 进程，或者直接在 GStreamer 管道中使用 `tee` 分流。
* **命令示例**：
```bash
ffmpeg -rtsp_transport tcp -i rtsp://xxx -c:v copy -f segment -segment_time 600 -reset_timestamps 1 /data/video/output_%03d.mp4

```


* 使用 `-c:v copy` 直接拷贝流，**不占用任何CPU/NPU资源**。


3. **内存泄漏防护**：
* Python的 `multiprocessing` 在长时间运行下可能会有微小的内存泄漏。
* 建议在 `main` 函数中加入逻辑：每隔 24 小时，优雅地重启一次子进程。


4. **RGA的使用**：
* 如果在Python中找不到好用的RGA库，可以将“读取+Resize”这一步封装成一个小的 C++ `.so` 动态库，用 Python `ctypes` 调用。这是性价比最高的优化，因为 Resize 是最大的CPU消耗源。



### 总结

* **结构**：主进程管理 + 摄像进程(GStreamer硬解) + 推理进程(RKNN)。
* **通信**：`SharedMemory` 传图，`Queue` 传结果。
* **瓶颈解决**：用 GStreamer 解决解码消耗，用 RGA 解决缩放消耗，用 NPU 解决推理消耗，Python 仅作为“胶水”逻辑。



1.5日进度
toolName: view_files
            
status: success
          
            
filePath: /home/hinlink/cleaningcar_v2/方案.md
          
这里先按「方案.md 里的理想方案」对比「现在项目的真实状态」，再总结我们已经做了哪些关键改进，方便你以后开新对话直接贴过去当背景。

---

**一、和《方案.md》对比：当前整体架构现状**

- 架构形态  
  - 方案.md：推荐「多进程 + SharedMemory」，把采集/存盘、推理、上报拆成 3 个进程。  
  - 现在项目：  
    - 实际是「单进程 + 多线程」的结构：`run_zone_detect.py` 作为主进程，下挂多个 `DetectWorker` 线程做 RKNN 推理，另外有上传队列等辅助线程。  
    - 没有单独的摄像进程 / 上报进程，也没有用 `multiprocessing.SharedMemory` 做跨进程零拷贝。但线程内的数据流已经比较紧凑，没有多余的大拷贝。

- 视频读取 / 解码  
  - 方案.md：要求 OpenCV+GStreamer，pipeline 必须走 `mppvideodec` 硬解，避免 CPU 软解。  
  - 现在项目：  
    - 已经按设计实现了「优先 GStreamer + mpp 硬解」：  
      日志里有  
      `Using GStreamer+mpp pipeline for /home/hinlink/下载/video/test03.mp4`  
    - 如果硬解失败，再回退到纯 OpenCV 的普通路径。  
    - 这一块基本符合方案设计。

- 预处理（最关键差异 & 已经补齐）  
  - 方案.md：明确强调「预处理用 RGA，不要用 `cv2.resize`，否则 CPU 会爆」。  
  - 初始现状：项目一开始是直接 `cv2.resize` 做 1080p→640×640 的缩放，CPU 很高。  
  - 现在状态（已大幅改进，见下一节）：
    - 已经实现并接入了基于 `librga.so` + C 封装 + Python 插件的 RGA 预处理路径；
    - `cv2.resize` 只有在 RGA 不可用或失败时才做兜底。

- 模型推理  
  - 方案.md：使用 `rknn-toolkit-lite2`，NPU 推理。  
  - 现在项目：  
    - 已经完全按方案使用 `rknnlite.api.RKNNLite`，并在多个 `DetectWorker` 线程中并发跑 NPU。  
    - 这一块和方案是一致的。

- 视频存盘 / 编码  
  - 方案.md：推荐用 GStreamer 或 ffmpeg 的硬编码（`mpph265enc` / `-c:v copy`），不要 Python 里 `cv2.VideoWriter`。  
  - 现在项目：  
    - 已经实现「per-id 视频」的编码逻辑，并且做了「优先硬编码」的策略：  
      - 优先使用 GStreamer 的 `h264_rkmpp` 做硬件编码；  
      - 如失败，再降级到 `libx264` 软编码。  
    - 没有在 Python 主循环里用 `cv2.VideoWriter` 写长视频，这点比方案要求还更实际（按需求做 per-id 小片段）。

- 进程通信 / 上报  
  - 方案.md：建议用 `SharedMemory` 传图、`Queue` 传结果，另外单独上报进程。  
  - 现在项目：  
    - 仍然是在单个 Python 进程内部，用队列和本地 SQLite 队列做上传管理；  
    - 功能上完全能工作，只是没有走到跨进程那一步（现阶段资源够用，所以不是瓶颈）。

---

**二、已经真正落地的关键改进**

1. **RGA 预处理从「纸面方案」变成「已上线能力」**

- 从方案到实现：
  - 方案.md 里只停留在“应该用 RGA / 可封装 C++ so”的建议；
  - 现项目已经完成了整个链路：
    - 系统安装 Rockchip 官方 `librga.so` + `im2d.h`；
    - C 封装：`rga_wrapper/rga_resize_wrapper.c`，提供 `rga_resize_bgr`，调用 `wrapbuffer_virtualaddr + imresize`；
    - Python 插件：`rga_resize_plugin.py` 用 `ctypes` 调这个 so；
    - 主流程：`run_zone_detect.py` 中通过 `RGA_RESIZE_FUNC` 扩展点优先使用 RGA 进行 letterbox resize。

- 安全性与正确性修正：
  - 一开始曾经因为 `wrapbuffer_virtualaddr` 参数写错（把 stride 塞在 format 位置）导致 `rga_buffer_t` 被清零，引发大量  
    `Invaild src buffer, no address available ...` / `imresize failed, ret=-3`。  
  - 现在已按官方 im2d 宏规范修正为：
    ```c
    rga_buffer_t src_buf = wrapbuffer_virtualaddr((void*)src, sw, sh, RK_FORMAT_BGR_888);
    rga_buffer_t dst_buf = wrapbuffer_virtualaddr((void*)dst, dw, dh, RK_FORMAT_BGR_888);
    ```
  - Python 侧每次调用都用独立的 `src` / `dst` numpy buffer，不共享 handle，不会出现多线程踩同一块内存的情况，避免了你之前 C++ RGA Demo 那次 kernel panic 那类问题。

- 性能效果（有实测数据支撑）：
  - 优化前：单路运行时 CPU 大约在 50% 左右；
  - 优化后：  
    - monitor 日志显示：`cpu=28.2% avg_cpu=24.5% max_cpu=36.4%` 量级；  
    - 等价于整体 CPU 峰值下降接近 30%，平均使用率几乎减半。  
  - 结合改动范围可判断：这主要就是 RGA 替换了 `cv2.resize` 带来的收益。

2. **部署脚本和依赖管理针对 RGA 做了落地优化**

- `install_runtime_venv.sh` 的改动：
  - 自动从项目内 `packages/` 目录把：
    - `librga.so` 安装到 `/usr/local/lib/librga.so`；
    - `im2d.h` 安装到 `/usr/local/include/rga/im2d.h`；
  - 启动前不再强制要求安装 `python3-rga`，而是改为提示：
    - 当前方案使用系统 `librga.so + rga_resize_plugin.py` 来实现 RGA 预处理。

- 针对无法 `git clone rkrga` 的环境：
  - 不再依赖在线 clone/编译 `rk-mirrors` 这类仓库；
  - 通过你在 PC 端打包好的 `librga.so + im2d.h` 直接部署，解决了内网/隔离环境下没法访问 GitHub 的问题。

3. **per-id 视频硬编码路径优化**

- 符合方案中「视频存盘不要压在 Python 上」的精神：
  - 引入 per-id 视频文件编码逻辑时：
    - 优先尝试 GStreamer pipeline + `h264_rkmpp` 做硬件编码；
    - 只有在硬编码不可用时，才回退到 `libx264` 软编码；
  - 避免在纯 Python 线程里用 `cv2.VideoWriter` 长时间写文件，减轻 GIL 与 CPU 压力。

- 结合现有 CPU 监控：
  - 在 RGA 生效的前提下，多路情况下 CPU 仍有富余，不会像旧方案那样轻易撑爆 100%。

4. **运行监控与稳定性增强**

- 新增了被动“观察窗口”：
  - 在 `run_zone_detect.py` 中增加了 monitor 输出：  
    `cpu=... avg_cpu=... mem=... uptime=... temp=...`  
  - 有利于你在现场直接通过日志观察长期运行状态（CPU、内存、温度）。

- 避免重复「内核崩溃」的问题：
  - 之前那次 kernel panic 是你自己用 C++ 多线程直接调用 RGA，存在：
    - 可能共享同一 buffer 或 handle；
    - 未严格保证尺寸/stride 合法；
    - 有 DMA 越界踩内核元数据风险。  
  - 现在的方案：
    - 每次调用使用独立 buffer；
    - 使用 im2d 官方 API 封装；
    - 调用是同步的，生命周期简单；  
    - 大幅降低了越界和竞态的风险，更适合在生产环境长期跑。

---

**三、还有哪些和《方案.md》不一样、但可以以后慢慢演进**

- 仍然是「单进程 + 多线程」，而不是「多进程 + SharedMemory」  
  - 当前性能和稳定性已经能满足两路、甚至更多路的需求；  
  - 如果以后要上 4–8 路，并且希望「推理/采集/上报」完全解耦，可以考虑再按方案把：
    - 摄像 + 存盘；
    - 推理；
    - 上报  
    拆成三个独立进程，用共享内存和队列串起来。

- 上报部分还没拆成单独进程  
  - 现在是主进程里有上传队列和 SQLite 做缓冲；  
  - 对当前业务是够用的，如果后面要做更复杂的网络策略（重试、限速、多目的地）可以考虑独立上报进程。

---

**四、小结：一句话概括当前状态**

- 解码：已经按方案用 GStreamer + mpp 硬解，失败再回退；
- 预处理：**RGA 预处理已实装并稳定运行，CPU 从 ~50% 降到 30%/20% 多**，`cv2.resize` 只做兜底；
- 推理：RKNNLite 多线程并发跑 NPU；
- 存盘：per-id 视频优先硬编码（`h264_rkmpp`），必要时软编码；
- 部署：`librga.so + im2d.h` 由脚本统一安装，适配无 GitHub 网络的环境；
- 架构：没有完全照方案拆多进程，但在「硬件加速 + 避免 Python 做重活」这条主线上，关键点（解码 / 预处理 / 推理 / 存盘）都已经对齐，并且有实际的性能收益和稳定性验证。